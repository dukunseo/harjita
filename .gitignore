# Dependencies
node_modules/

# Wrangler local state & cache (termasuk data D1/R2 lokal dari `wrangler dev`)
.wrangler/

# Secret & env lokal — JANGAN PERNAH commit file ini kalau suatu saat dibuat
.dev.vars
.dev.vars.*
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build output (kalau nanti ditambah build step)
dist/
build/

# Database sementara hasil `node smoketest/*.mjs` (bukan bagian dari kode)
smoketest/*.sqlite
